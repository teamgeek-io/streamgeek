/**
 * @module
 *
 * All of our orchestrator api endpoints live here.
 * These are endpoints that are called by the agents.
 *
 */

import { route } from "rwsdk/router";

// export const orchestratorEndpoints = [
//   route("/orchestrator/create-job", createJob),
// ];
