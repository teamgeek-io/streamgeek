{
  "name": "streamgeek",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:app": "pnpm --filter app dev",
    "build:app": "pnpm --filter app build",
    "start:app": "pnpm --filter app start",
    "lint:app": "pnpm --filter app lint",
    "dev:api": "pnpm --filter api dev",
    "build:api": "pnpm --filter api build",
    "start:api": "pnpm --filter api start",
    "lint:api": "pnpm --filter api lint"
  },
  "homepage": "https://github.com/teamgeek-io/streamgeek#readme",
  "repository": {
    "type": "git",
    "url": "https://github.com/teamgeek-io/streamgeek.git"
  },
  "bugs": {
    "url": "https://github.com/teamgeek-io/streamgeek/issues"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5"
  }
}
