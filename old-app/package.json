{
  "name": "streamgeek",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "app:dev": "pnpm --filter streamgeek-app dev",
    "app:build": "pnpm --filter streamgeek-app build",
    "app:start": "pnpm --filter streamgeek-app start",
    "app:lint": "pnpm --filter streamgeek-app lint",
    "app:preview": "pnpm --filter app preview",
    "app:deploy": "pnpm --filter streamgeek-app deploy",
    "app:cf-typegen": "pnpm --filter streamgeek-app cf-typegen",
    "app:db:generate": "pnpm --filter streamgeek-app db:generate",
    "app:db:migrate": "pnpm --filter streamgeek-app db:migrate",
    "api:dev": "pnpm --filter api dev",
    "api:build": "pnpm --filter api build",
    "api:start": "pnpm --filter api start",
    "api:lint": "pnpm --filter api lint"
  },
  "homepage": "https://github.com/teamgeek-io/streamgeek#readme",
  "repository": {
    "type": "git",
    "url": "https://github.com/teamgeek-io/streamgeek.git"
  },
  "bugs": {
    "url": "https://github.com/teamgeek-io/streamgeek/issues"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5"
  }
}
